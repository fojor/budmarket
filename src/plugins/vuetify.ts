import Vue from 'vue';
import Vuetify from 'vuetify';
import {
    VBtn,
    VDialog
} from 'vuetify/lib';

Vue.use(Vuetify, {
    components: {
        VBtn,
        VDialog
    }
});

export default new Vuetify({
});
